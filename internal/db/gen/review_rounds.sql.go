// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: review_rounds.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const currentReviewRound = `-- name: CurrentReviewRound :one
SELECT id, work_branch_id, number, requested_by, created_at FROM review_rounds
WHERE work_branch_id = $1
ORDER BY number DESC
LIMIT 1
`

// The work branch's current round: the review_rounds row with the highest
// number. Every "is this stale" comparison in this table group goes
// through here or the equivalent inline subquery below -- never through a
// stored flag.
func (q *Queries) CurrentReviewRound(ctx context.Context, workBranchID pgtype.UUID) (ReviewRound, error) {
	row := q.db.QueryRow(ctx, currentReviewRound, workBranchID)
	var i ReviewRound
	err := row.Scan(
		&i.ID,
		&i.WorkBranchID,
		&i.Number,
		&i.RequestedBy,
		&i.CreatedAt,
	)
	return i, err
}

const currentRoundApproveCount = `-- name: CurrentRoundApproveCount :one
SELECT COUNT(*) FROM verdicts v
JOIN review_rounds r ON r.id = v.round_id
WHERE r.work_branch_id = $1
AND r.number = (SELECT MAX(r2.number) FROM review_rounds r2 WHERE r2.work_branch_id = $1)
AND v.outcome = 'approve'
`

// Approve-outcome count for the work branch's current round only -- backs
// the proposal queue / approval bar (docs/persistence-spec.md "verdicts").
// Joins on the current round via a MAX(number) subquery rather than
// trusting any stored staleness flag; when the branch has no rounds yet
// the subquery is NULL, no row ever matches, and the count is simply 0.
func (q *Queries) CurrentRoundApproveCount(ctx context.Context, workBranchID pgtype.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, currentRoundApproveCount, workBranchID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const listVerdictsForWorkBranch = `-- name: ListVerdictsForWorkBranch :many
SELECT
    v.id, v.round_id, r.number AS round_number, v.reviewer, v.outcome,
    v.created_at, v.updated_at,
    (r.number = (SELECT MAX(r2.number) FROM review_rounds r2 WHERE r2.work_branch_id = $1)) AS is_current_round
FROM verdicts v
JOIN review_rounds r ON r.id = v.round_id
WHERE r.work_branch_id = $1
ORDER BY r.number DESC, v.created_at ASC
`

type ListVerdictsForWorkBranchRow struct {
	ID             pgtype.UUID
	RoundID        pgtype.UUID
	RoundNumber    int32
	Reviewer       string
	Outcome        string
	CreatedAt      pgtype.Timestamptz
	UpdatedAt      pgtype.Timestamptz
	IsCurrentRound bool
}

// All verdicts across all rounds for a work branch, newest round first, for
// history. is_current_round is computed by the same query that reads each
// row (comparing round numbers against the branch's current MAX(number)),
// never read from a stored column -- staleness is derived, not stored
// (docs/persistence-spec.md "verdicts").
func (q *Queries) ListVerdictsForWorkBranch(ctx context.Context, workBranchID pgtype.UUID) ([]ListVerdictsForWorkBranchRow, error) {
	rows, err := q.db.Query(ctx, listVerdictsForWorkBranch, workBranchID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListVerdictsForWorkBranchRow
	for rows.Next() {
		var i ListVerdictsForWorkBranchRow
		if err := rows.Scan(
			&i.ID,
			&i.RoundID,
			&i.RoundNumber,
			&i.Reviewer,
			&i.Outcome,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.IsCurrentRound,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const openReviewRound = `-- name: OpenReviewRound :one
INSERT INTO review_rounds (id, work_branch_id, number, requested_by)
SELECT $1, $2, COALESCE(MAX(number), 0) + 1, $3
FROM review_rounds
WHERE work_branch_id = $2
RETURNING id, work_branch_id, number, requested_by, created_at
`

type OpenReviewRoundParams struct {
	ID           pgtype.UUID
	WorkBranchID pgtype.UUID
	RequestedBy  string
}

// Opens a new review round for a work branch: number = MAX(number)+1 for
// that work_branch_id (1 if none exist yet). A single atomic
// INSERT ... SELECT so the number computation and the row creation happen
// as one statement -- review_rounds_work_branch_id_number_key still
// catches a concurrent-open race as a real unique-violation rather than
// silently double-assigning a round number (docs/persistence-spec.md
// "review_rounds"; called on every transition into reviewable: author
// request-review, admin send-back, catch-up auto-restore).
func (q *Queries) OpenReviewRound(ctx context.Context, arg OpenReviewRoundParams) (ReviewRound, error) {
	row := q.db.QueryRow(ctx, openReviewRound, arg.ID, arg.WorkBranchID, arg.RequestedBy)
	var i ReviewRound
	err := row.Scan(
		&i.ID,
		&i.WorkBranchID,
		&i.Number,
		&i.RequestedBy,
		&i.CreatedAt,
	)
	return i, err
}

const submitVerdict = `-- name: SubmitVerdict :one
INSERT INTO verdicts (id, round_id, reviewer, outcome)
VALUES ($1, $2, $3, $4)
ON CONFLICT (round_id, reviewer)
DO UPDATE SET outcome = EXCLUDED.outcome, updated_at = now()
RETURNING id, round_id, reviewer, outcome, created_at, updated_at
`

type SubmitVerdictParams struct {
	ID       pgtype.UUID
	RoundID  pgtype.UUID
	Reviewer string
	Outcome  string
}

// Re-submitting the same (round_id, reviewer) replaces the prior verdict
// in place (upsert on conflict), never creating a second row --
// verdicts_round_id_reviewer_key is the constraint Demo M1 shows off: one
// verdict per reviewer per round.
func (q *Queries) SubmitVerdict(ctx context.Context, arg SubmitVerdictParams) (Verdict, error) {
	row := q.db.QueryRow(ctx, submitVerdict,
		arg.ID,
		arg.RoundID,
		arg.Reviewer,
		arg.Outcome,
	)
	var i Verdict
	err := row.Scan(
		&i.ID,
		&i.RoundID,
		&i.Reviewer,
		&i.Outcome,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
