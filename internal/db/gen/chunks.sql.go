// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: chunks.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
	pgvector "github.com/pgvector/pgvector-go"
)

const insertChunk = `-- name: InsertChunk :one
INSERT INTO chunks (id, repo_id, target_branch, file, start_line, end_line, content, embedding)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, repo_id, target_branch, file, start_line, end_line, content, embedding, created_at
`

type InsertChunkParams struct {
	ID           pgtype.UUID
	RepoID       pgtype.UUID
	TargetBranch string
	File         string
	StartLine    int32
	EndLine      int32
	Content      string
	Embedding    pgvector.Vector
}

// Exercises sqlc's pgvector type override (sqlc.yaml) on the write side:
// $8 must bind as pgvector.Vector.
func (q *Queries) InsertChunk(ctx context.Context, arg InsertChunkParams) (Chunk, error) {
	row := q.db.QueryRow(ctx, insertChunk,
		arg.ID,
		arg.RepoID,
		arg.TargetBranch,
		arg.File,
		arg.StartLine,
		arg.EndLine,
		arg.Content,
		arg.Embedding,
	)
	var i Chunk
	err := row.Scan(
		&i.ID,
		&i.RepoID,
		&i.TargetBranch,
		&i.File,
		&i.StartLine,
		&i.EndLine,
		&i.Content,
		&i.Embedding,
		&i.CreatedAt,
	)
	return i, err
}

const searchChunksByEmbedding = `-- name: SearchChunksByEmbedding :many
SELECT id, repo_id, target_branch, file, start_line, end_line, content, embedding, created_at FROM chunks
WHERE repo_id = $1 AND target_branch = $2
ORDER BY embedding <=> $3
LIMIT $4
`

type SearchChunksByEmbeddingParams struct {
	RepoID       pgtype.UUID
	TargetBranch string
	Embedding    pgvector.Vector
	Limit        int32
}

// RAG search per docs/persistence-spec.md:153: nearest-neighbour over the
// chunks_embedding HNSW index (0002_code_intel.up.sql), ordered by cosine
// distance. Exercises the pgvector override on the read/parameter side.
func (q *Queries) SearchChunksByEmbedding(ctx context.Context, arg SearchChunksByEmbeddingParams) ([]Chunk, error) {
	rows, err := q.db.Query(ctx, searchChunksByEmbedding,
		arg.RepoID,
		arg.TargetBranch,
		arg.Embedding,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Chunk
	for rows.Next() {
		var i Chunk
		if err := rows.Scan(
			&i.ID,
			&i.RepoID,
			&i.TargetBranch,
			&i.File,
			&i.StartLine,
			&i.EndLine,
			&i.Content,
			&i.Embedding,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
