import type { ReactElement } from "react";

/** Credentials (`/credentials`) — per-forge-host tokens. Filled in by loam-nvb.10. */
export function Credentials(): ReactElement {
  return <h1>Credentials</h1>;
}
