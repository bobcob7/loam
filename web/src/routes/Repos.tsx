import type { ReactElement } from "react";

/**
 * Repos — the default screen (`/`): enrolled repos with sync status and the
 * enroll form (docs/web-frontend-spec.md -> Routing & Screens).
 *
 * A placeholder heading until loam-nvb.8 fills it in. The shell owns the
 * route and the layout; every screen module in this directory is the landing
 * spot for exactly one screen bead.
 */
export function Repos(): ReactElement {
  return <h1>Repos</h1>;
}
